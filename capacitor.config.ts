import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samektra.compliancelens',
  appName: 'Samektra Compliance Lens',
  webDir: 'dist'
};

export default config;
